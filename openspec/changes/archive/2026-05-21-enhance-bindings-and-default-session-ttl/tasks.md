## 1. 绑定编辑后端能力

- [x] 1.1 在绑定仓储中增加按 `botId + bindingId` 更新 `chatName`、`chatType`、`contextId` 的方法，并禁止更新 `chatKey`
- [x] 1.2 在绑定 API 中增加编辑接口，校验请求字段和目标上下文归属
- [x] 1.3 在 Bot 运行时管理中增加绑定更新同步能力，使运行中 Bot 在编辑后立即使用新绑定配置
- [x] 1.4 为绑定编辑成功、绑定不存在、上下文无效、`chatKey` 不可修改等场景补充测试

## 2. 绑定编辑前端体验

- [x] 2.1 在前端 API 客户端中增加绑定编辑调用
- [x] 2.2 在绑定管理页为已绑定记录增加编辑入口，复用或扩展现有表单
- [x] 2.3 编辑表单预填 `chatKey`、`chatName`、`chatType`、`contextId`，并将 `chatKey` 展示为不可编辑
- [x] 2.4 编辑保存成功后刷新绑定列表，并验证后续消息会路由到新的上下文

## 3. 平台默认会话 TTL 配置

- [x] 3.1 增加平台默认会话 TTL 的配置读取和校验能力，保留安全的系统兜底值
- [x] 3.2 调整上下文创建后端逻辑：未提供 `sessionTtlMin` 时使用平台默认 TTL，显式提供时保留请求值
- [x] 3.3 调整上下文创建前端表单：初始 `sessionTtlMin` 使用平台默认 TTL
- [x] 3.4 为默认 TTL 优先级、合法性校验、已有上下文不被覆盖补充测试

## 4. 验证与回归

- [x] 4.1 运行相关单元测试和类型检查
- [x] 4.2 手动验证绑定编辑流程：编辑名称、类型、上下文，确认 `chatKey` 不可修改
- [x] 4.3 手动验证默认 TTL 流程：修改平台默认值后新建上下文使用新默认值，已有上下文和存量 session 不变化
- [x] 4.4 检查会话监控页剩余 TTL 显示仍基于 `expiresAt`，未改变滑动续期语义
